@model OrderModel

@{
    ViewData["Title"] = "Ваш заказ оформлен";
}

Ваш заказ #@Model.Id оформлен и будет передан в службу доставки с течение одного-двух дней.
Как только это случится, мы отправим вам SMS.

@await Html.PartialAsync("PartialDetails", Model)

